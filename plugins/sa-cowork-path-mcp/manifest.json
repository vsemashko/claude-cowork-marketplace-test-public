{
  "manifest_version": "0.3",
  "name": "sa-cowork-path-mcp",
  "display_name": "Cowork PATH Probe",
  "version": "1.0.0",
  "description": "Baseline manifest plugin that only succeeds when the probe executable already exists on PATH.",
  "author": {
    "name": "StashAway Engineering",
    "url": "https://github.com/vsemashko"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vsemashko/claude-cowork-marketplace-test-public"
  },
  "server": {
    "type": "node",
    "entry_point": "server/path-probe-server.mjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/path-probe-server.mjs"],
      "env": {
        "PROBE_STRATEGY": "path",
        "PROBE_PLUGIN_NAME": "sa-cowork-path-mcp",
        "PROBE_PLUGIN_VERSION": "1.0.0",
        "PROBE_PLUGIN_ROOT": "${__dirname}",
        "PROBE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}",
        "PROBE_COMMAND_NAME": "cowork-probe-cli",
        "PROBE_LABEL": "${user_config.probe_label}",
        "PROBE_ENDPOINT": "${user_config.probe_endpoint}",
        "PROBE_TOKEN": "${user_config.probe_token}"
      }
    }
  },
  "tools": [
    { "name": "report_env", "description": "Report probe_label and probe_endpoint verbatim, plus probe_token as a redacted length." },
    { "name": "run_probe", "description": "Execute the PATH-only probe and report the resolved executable path or a clean failure." },
    { "name": "report_cache", "description": "Report the plugin-data cache location and explain that PATH mode does not reuse cached assets." }
  ],
  "user_config": {
    "probe_label": {
      "type": "string",
      "title": "Probe Label",
      "description": "Human-readable label echoed by report_env and run_probe.",
      "required": false,
      "default": "PATH Probe"
    },
    "probe_endpoint": {
      "type": "string",
      "title": "Probe Endpoint",
      "description": "Endpoint value used to verify non-sensitive config propagation.",
      "required": false,
      "default": "https://example.invalid/path-probe"
    },
    "probe_token": {
      "type": "string",
      "title": "Probe Token",
      "description": "Sensitive token used to verify redaction behavior.",
      "required": false,
      "sensitive": true
    }
  },
  "keywords": ["cowork", "test", "path", "manifest", "mcp"],
  "license": "MIT",
  "compatibility": {
    "claude_desktop": ">=0.11.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": { "node": ">=18.0.0" }
  }
}
