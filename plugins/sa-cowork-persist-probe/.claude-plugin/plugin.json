{
  "name": "sa-cowork-persist-probe",
  "version": "2.82.0",
  "description": "Minimal Cowork probe plugin that writes and reads a file from CLAUDE_PLUGIN_DATA, reports its own config, and can inspect an explicit bridge file exported by the persist extension",
  "author": {
    "name": "StashAway Engineering"
  },
  "userConfig": {
    "probe_label": {
      "type": "string",
      "title": "Probe Label",
      "description": "Label prefix for persisted values (e.g. 'QWE')",
      "default": "QWE"
    },
    "probe_secret": {
      "type": "string",
      "title": "Probe Secret",
      "description": "Secret token to verify sensitive storage works",
      "sensitive": true
    }
  }
}
