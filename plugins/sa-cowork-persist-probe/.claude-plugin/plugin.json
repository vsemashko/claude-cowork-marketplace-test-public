{
  "name": "sa-cowork-persist-probe",
  "version": "2.81.0",
  "description": "Minimal Cowork probe plugin that writes and reads a file from CLAUDE_PLUGIN_DATA and can inspect an explicit bridge file exported by the MCPB persist probe",
  "author": {
    "name": "StashAway Engineering"
  },
  "userConfig": {
    "probe_label": {
      "type": "string",
      "title": "Probe Label",
      "description": "Label prefix for persisted values (e.g. 'QWE')",
      "default": "QWE"
    },
    "probe_secret": {
      "type": "string",
      "title": "Probe Secret",
      "description": "Secret token to verify sensitive storage works",
      "sensitive": true
    }
  }
}
