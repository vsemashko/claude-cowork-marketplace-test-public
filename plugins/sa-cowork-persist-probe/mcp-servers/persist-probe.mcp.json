{
  "command": "node",
  "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-servers/persist-probe-server.mjs"],
  "env": {
    "PROBE_LABEL": "${user_config.probe_label}",
    "PROBE_SECRET": "${user_config.probe_secret}",
    "PERSIST_STORE_DIR": "${CLAUDE_PLUGIN_DATA}/persist-probe"
  },
  "_meta": {
    "name": "persist-probe",
    "description": "MCP server for writing/reading persisted values. Uses userConfig for probe_label and probe_secret.",
    "enabled": true
  }
}
